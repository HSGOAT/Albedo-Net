# requirements-ci.txt — AlbedoNet
# ==================================
# Sous-ensemble STRICT de requirements.txt, suffisant pour lancer
# `pytest tests/` (cf. .github/workflows/data-and-thresholds-audit.yml).
#
# Volontairement separe de requirements.txt complet : les tests/ actuels
# (splits, coherence cles, smoke par ville, sync confidence/materials,
# regression classifieur) n'importent jamais geopandas/rasterio/shapely/
# pyproj/fastapi (ces libs geo dependent de GDAL, absent par defaut sur le
# runner GitHub Actions -- les ajouter ici ralentirait la CI et risquerait
# de la casser pour rien). Si un futur test importe main.py/zone_scan.py/
# ign_fetch.py directement, migrer ce test vers `pip install -r
# requirements.txt` complet plutot que d'alourdir ce fichier au cas par
# cas.
#
# IMPORTANT : si vous ajoutez une dependance ici, ajoutez-la (ou verifiez
# qu'elle y est deja) dans requirements.txt egalement -- requirements.txt
# reste la source de verite des VERSIONS, celui-ci ne fait que restreindre
# la LISTE pour la CI.

numpy>=1.26
pandas>=2.2
pyarrow>=15.0
scikit-learn>=1.4
scikit-image>=0.22
opencv-python-headless>=4.9
joblib>=1.3
pytest>=8.0
torch>=2.2
